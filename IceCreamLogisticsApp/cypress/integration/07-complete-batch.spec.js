describe('Complete batch', () => {
  it('Load the app', () => {
    cy.visit('/');
    cy.login();
  });
  it('Navigate to batch', () => {
  });
  it('Set data', () => {
  });
  it('Save', () => {
  });
});
